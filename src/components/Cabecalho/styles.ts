import styled from 'styled-components'

export const CabecalhoStyle = styled.div`
  background-color: ${(props) => props.theme.corSecundariag};
  color: ${(props) => props.theme.corPrincipal};
  text-align: center;
  padding: 24px 0;
`
